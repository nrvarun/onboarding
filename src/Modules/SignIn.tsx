import AuthLayout from "Layout/AuthLayout";

type Props = {};

const SignIn = (props: Props) => {
  return <AuthLayout title="Login to Hiki Account">*login form*</AuthLayout>;
};

export default SignIn;
